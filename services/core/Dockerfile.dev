FROM node:20-alpine

WORKDIR /core

COPY package*.json ./
COPY tsconfig.json ./

RUN npm ci

# Install nodemon globally for better process management (optional, already in devDependencies)
CMD ["npm", "run", "dev"]

